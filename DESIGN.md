# Obsidian 腾讯文档发布插件设计

状态：实现基线 0.4；首版代码与自动化测试已完成，待真实 Vault 和腾讯测试文档验收
暂定名称：Tencent Docs Publisher / 腾讯文档发布器
仓库：https://github.com/totok22/tencent-docs-publisher

## 1. 产品定位

插件用于把 Obsidian 中的一篇 Markdown，或由“总览 Markdown”组织的一组 Markdown，手动发布到一个腾讯智能文档及其已有子页面。

- Obsidian 是内容源，腾讯智能文档是展示端。
- 只处理 Markdown，以及 Markdown 实际引用的本地图片和 PDF。
- 不扫描或上传没有被 Markdown 引用的附件。
- 不处理 Excel、Word、PPT、音视频、Git 钩子或定时发布。
- 默认单向发布，不把腾讯文档内容自动合并回本地。
- 只由用户手动触发，不监听保存事件，不接入 Git 钩子，也不在后台定时发布。
- 插件名称、命令、设置、日志和示例保持通用，不包含具体组织或业务名称。

第一版解决的是“本地内容树 → 已有远端页面树”的可靠映射和更新，不试图复刻完整的云盘或知识库。

## 2. 核心对象

### 2.1 单页发布

一个本地 Markdown 绑定到：

- 一个已有或由插件新建的独立智能文档根页面；或
- 某个智能文档中的一个已有子页面。

适合只需要发布一篇笔记的情况。

### 2.2 总览发布树

用户选择一篇 Markdown 作为“总览文件”。插件按总览文件中的本地 Markdown 链接构建页面树：

```text
总览.md
├─ [[安装.md]]
├─ [[使用方法.md]]
│  ├─ [[基础操作.md]]
│  └─ [[高级功能.md]]
└─ [[常见问题.md]]
```

对应远端结构：

```text
智能文档根页面
├─ 安装
├─ 使用方法
│  ├─ 基础操作
│  └─ 高级功能
└─ 常见问题
```

总览文件决定入口、顺序和可达范围。插件不会因为某个文件恰好位于同一文件夹就自动发布它。

### 2.3 文件夹入口

文件夹右键是总览发布树的快捷入口，不代表“发布文件夹里的所有 Markdown”。插件按以下顺序寻找总览文件：

1. `README.md`；
2. `index.md`；
3. 与文件夹同名的 `.md`；
4. 如果没有唯一候选，让用户从该文件夹内选择。

用户确认后，该文件成为发布树的根节点。

## 3. 本地页面树发现规则

### 3.1 使用 Obsidian 的解析结果

插件使用 `MetadataCache` 和 `getFirstLinkpathDest` 解析链接，不使用简单正则表达式猜文件路径。这样可以正确处理：

- `[[文档]]`；
- `[[目录/文档]]`；
- `[[文档|显示名称]]`；
- 文件重命名后的链接；
- 同名文件的 Obsidian 路径解析规则。

页面顺序取父 Markdown 中链接第一次出现的位置，包含表格、列表和普通段落中的链接。这样总览表格本身就可以承担目录排序功能。

### 3.2 哪些链接形成子页面

默认规则：

| 本地写法 | 页面树行为 |
| --- | --- |
| `[[文档]]` | 若解析为范围内的完整 Markdown，成为子页面候选 |
| `[[文档|别名]]` | 同上；别名只影响正文显示，不改变本地文件身份 |
| `[文字](文档.md)` | 若解析为范围内的本地 Markdown，也成为子页面候选 |
| `[[文档#标题]]` | 普通章节引用，不创建独立页面节点 |
| `[[文档#^块]]` | 普通块引用，不创建独立页面节点 |
| `![[文档]]` | 默认作为内嵌内容，不创建远端子页面；可在项目设置中改为子页面 |
| 图片链接或嵌入 | 资源依赖，不是页面节点 |
| PDF 链接或嵌入 | 资源依赖，不是页面节点 |
| 网页链接 | 原样保留，不进入页面树 |

选择总览文件时，同时确定“允许跟随的根文件夹”。链接到根文件夹外的 Markdown 默认保留为普通引用，不继续递归，防止一次发布意外遍历整个 Vault。

### 3.3 递归规则

插件按深度优先方式递归：

1. 读取父页面中的链接，按出现顺序排列；
2. 解析得到直接子 Markdown；
3. 再读取每个子 Markdown，得到下一层；
4. 直到没有新的子页面或达到安全上限。

默认最大深度为 8，默认最多 200 篇 Markdown。达到上限时停止并在预览中显示，不静默截断。

### 3.4 循环与多父引用

本地链接本质上是图，腾讯子页面是树，因此需要确定唯一父级：

- 出现 `A → B → A` 时识别为循环，不再次展开 A；
- 同一 Markdown 被多个父页面链接时，第一次遍历到的位置成为页面归属；
- 后续引用仍保留显示文字，但不再创建第二个页面映射；
- 预览中列出循环和多父引用，让用户可以调整总览结构或手动确认归属。

插件以 Vault 相对路径作为本地页面身份，不使用文件名作为唯一键。

## 4. 远端页面树发现规则

### 4.1 已验证能力

当前腾讯接口支持：

- 获取智能文档的根 Page；
- 通过 `smartcanvas.read(file_id, page_id)` 读取指定页面；
- 父页面内容中的直接子页面以 `<Page id="...">` 形式出现；
- 递归读取子页面，从而发现更深层页面；
- 使用页面内真实 Block ID 修改、删除或在其前后插入内容。

实际测试已确认：向已有子页面的 Paragraph 后插入内容，内容会准确出现在该子页面；随后可以回读 Block ID 并删除恢复。

### 4.2 当前限制

当前接口没有可靠的子页面创建、重命名、移动和删除能力。将 `<Page>` 作为 MDX 写入只会得到普通文本，不会创建新页面。

因此：

- 插件可以递归识别和更新已有子页面；
- 缺少对应远端页面时，用户需要先在腾讯文档界面中手动创建；
- 创建后在插件中执行“刷新远端页面树”，再完成绑定；
- 插件不得用未公开网页接口模拟页面创建。

### 4.3 递归读取算法

```text
get_top_level_pages(file_id)
└─ root_page_id
   └─ read(file_id, root_page_id)
      ├─ 普通内容块
      ├─ Page child_A
      │  └─ read(file_id, child_A)
      │     ├─ 普通内容块
      │     └─ Page grandchild_A1
      └─ Page child_B
```

解析页面时只把当前 Page 的直接 `<Page>` 子元素作为下一层，不能把更深层 Page 错当成同级。所有递归都需要 Page ID 去重和深度保护。

`smartcanvas.find` 不能可靠搜索子页面内容，因此页面定位必须依赖保存的 Page ID 和指定页面回读，不能依赖全局文字搜索。

`smartcanvas.read` 是分页接口，`size` 最大为 20。每次读取一个 Page 时必须持续传入返回的 `next_token`，直到游标为空，再拼接该 Page 的完整内容。任何树发现、冲突检查、备份或发布后验证都不得只读取第一页。

`smartcanvas.get_top_level_pages` 返回的 `children` 可能同时包含普通 Block ID 与 Page ID，不能直接把该数组当作子页面列表。页面层级以完整回读内容中的 `<Page>` 嵌套关系为准。

远端树节点至少保存：

```ts
interface RemotePageNode {
  pageId: string;
  parentPageId: string | null;
  title: string;
  childPageIds: string[];
  contentFingerprint: string;
}
```

标题从父页面中的直接 `<Page>` 包装块解析；根页面标题取 `manage.query_file_info` 的文档标题。解析器必须能够处理 Page 块内继续嵌套 Page 块的情况。

### 4.4 能力状态

后续实现不得把接口推测写成已支持能力。能力分为三类：

- **已验证**：指定 Page 分页读取、现有子页面内容编辑、图片上传、PDF 导入、设置全员可读；
- **受限**：公开状态缺少可靠的只读查询接口，只能确认权限设置请求成功，再由用户从未登录窗口抽查；
- **不支持**：自动创建、移动、重命名、删除子页面，以及获取稳定的子页面直达 URL。

腾讯接口能力变化时，只修改能力适配层，不改变本地项目和绑定数据的含义。

## 5. 本地树与远端树的绑定

### 5.1 绑定数据

```ts
interface PublishProject {
  schemaVersion: number;
  id: string;
  accountId: string;
  sourceRootPath: string;
  allowedRootPath: string;
  remoteFileId: string;
  remoteUrl: string;
  remoteRootPageId: string;
  publicRead: boolean;
  maxDepth: number;
  maxNotes: number;
  pageMap: Record<string, RemotePageBinding>;
}

interface RemotePageBinding {
  pageId: string;
  parentPageId: string | null;
  localTitle: string;
  remoteTitle: string;
  lastPublishedSourceHash?: string;
  lastPublishedRemoteHash?: string;
  lastPublishedAt?: string;
}
```

Token 单独存入 Obsidian `SecretStorage`。项目和页面映射保存在插件 `data.json`，不在 Markdown 中加入同步标记。

`accountId` 来自连接测试的当前用户信息。替换 Token 后如果账号发生变化，已有项目保持但进入“账号待确认”状态，在重新验证目标文档访问权前禁止写入，避免把旧账号绑定误用于新账号。

所有持久化数据带 `schemaVersion`，插件启动时执行显式迁移。未知的新版本数据不得被旧版插件覆盖。

### 5.2 初次绑定

绑定界面显示左右两棵树：

```text
本地总览树                         腾讯页面树
README                             根页面
├─ 安装                 ← 已匹配 → ├─ 安装
├─ 使用方法             ← 已匹配 → ├─ 使用方法
│  ├─ 基础操作          ← 已匹配 → │  ├─ 基础操作
│  └─ 高级功能          ← 缺少   → │  └─ （无）
└─ 常见问题             ← 待确认 → └─ FAQ
```

自动匹配优先级：

1. 已保存的 Page ID；
2. 同一父页面下，远端标题与 frontmatter `title` 完全一致；
3. 同一父页面下，远端标题与文件名完全一致；
4. 同一父页面下，远端标题与本地 aliases 唯一匹配；
5. 其余情况要求用户手动选择。

同名结果不自动选择。父级不同也不能只因标题相同而匹配。

### 5.3 缺失页面

本地节点没有远端页面时显示“待创建”，并提供：

- `打开所属腾讯文档`，并显示应创建到哪个父页面标题下；
- 简短提示用户手动创建同名子页面；
- `刷新远端页面树`；
- 刷新后自动重试匹配。

缺失页面默认阻止整个树的正式发布，也可以在预览中取消勾选该分支后发布其余已绑定页面。

### 5.4 远端多余页面

远端存在但本地树没有对应节点时标记为“未绑定”，插件不读取其附件、不修改内容，也不删除页面。

如果已保存的 Page ID 仍存在但父级发生变化，标记为“远端层级已变更”，不得仅凭同名标题自动改绑。用户确认后才更新 `parentPageId`。

## 6. 页面内容更新

### 6.1 必须保留子页面块

每个页面的回读内容可能同时包含：

- 本页面的普通内容块；
- 直接子页面 `<Page>` 块；
- 只读或暂不支持的块。

同步时只替换当前页面的普通内容。以下内容必须保留：

- `<Page>` 子页面块；
- 标记为 `readonly` 的块；
- 插件不能安全识别的 `Unsupported` 块。

这条规则适用于根页面和任意深度的子页面，否则同步父文档会把整个远端导航树破坏掉。

### 6.2 写入锚点

插件先读取指定 Page 并取得真实 Block ID：

- 页面有普通内容时，保留一个可写块作为锚点；
- 页面没有普通内容但有子页面时，可使用第一个子 Page 块作为插入前后的定位锚点，但不修改 Page 块本身；
- 页面完全空白且接口没有返回任何可用块时，提示用户先在该页面输入任意占位文字，再刷新重试。

不使用 `id=""` 向子页面追加，因为空 ID 无法明确指定目标 Page，可能写入根页面。

### 6.3 页面内子页面的位置

腾讯接口能够保留已有 Page 块，但不能根据本地 wikilink 自动生成或移动 Page 块。因此第一版保证页面层级正确，不保证子页面卡片精确出现在父 Markdown 对应链接所在的表格单元格或段落中。

本地 wikilink 在正文中保留显示文字；远端已有子页面卡片继续保留原位置。后续只有在腾讯接口能返回稳定的页面跳转 URL 或支持 Page 块移动时，才增强为可点击的精确位置链接。

### 6.4 远端人工修改

接口没有稳定暴露每个子 Page 的独立修改时间，因此冲突检测使用远端内容指纹，不依赖文件级 `last_modify_time`：

1. 发布成功后，对该 Page 完整分页回读；
2. 从回读内容中剔除直接子 Page 包装块的可变展示信息，但保留 Page ID、只读块和普通内容；
3. 规范化换行和无语义空白，保存为 `lastPublishedRemoteHash`；
4. 下次发布前再次完整回读并计算指纹；
5. 当前指纹与已保存指纹不同则标记为远端冲突。

文件级修改时间只可作为“可能有变化”的提示，不能证明具体子页面是否被修改。发生冲突时提供：

- 打开远端页面；
- 跳过该页面；
- 查看远端回读内容；
- 明确确认后覆盖一次。

第一版不提供双向合并，因为腾讯 MDX 与原始 Obsidian Markdown 不是无损互逆。

### 6.5 非事务写入与失败恢复

`smartcanvas.edit` 是多次 Block 操作，不具备整页事务。发布器必须：

- 串行执行同一 Page 的写操作；
- 每次删除前在本次任务内保留该 Page 的完整远端快照；
- 网络状态不明确时不自动重放写请求，先回读判断操作是否已生效；
- 任一步失败立即停止该 Page 后续写入，并尝试用快照恢复；
- 无法确认或恢复时标记为“远端状态未知”，要求用户打开页面检查，不继续发布其父页面；
- 只有发布后完整回读和结构校验通过，才更新本地发布哈希。

本地快照只用于当前任务的尽力恢复，不宣称提供永久版本历史；长期版本恢复依赖腾讯文档自身历史记录。

## 7. Markdown 转换、图片与 PDF

### 7.1 Markdown 转换契约

转换器以 Obsidian 实际解析结果为准，并明确处理以下内容：

| 本地内容 | 远端行为 |
| --- | --- |
| YAML properties | 用于读取 `title`、`aliases` 等元数据，默认不发布正文 |
| 标题、段落、列表、引用、代码块 | 转换为智能文档支持的 Markdown/MDX |
| Markdown 表格 | 转换为 `<Table>` 组件 |
| 行内与块级公式 | 保持公式语义；块级公式按需要使用 `<MathBlock>` |
| Obsidian callout | 转为受支持的 `<Callout>`；无法映射的类型使用通用样式 |
| wikilink | 解析本地目标；页面链接显示别名或标题，不伪造远端 URL |
| Markdown 嵌入 | 按项目设置内嵌或形成页面节点，并做循环保护 |
| HTML、Dataview、Canvas 等插件内容 | 第一版不执行；预览中警告并按纯文本或占位说明处理 |
| Obsidian 注释 `%%...%%` | 不发布 |

写入已有 Page 时生成的是 Block 片段，不附加整篇文档 frontmatter，也不在正文开头重复页面标题。转换结果进入 `smartcanvas.edit` 前必须通过本地 MDX 白名单校验：禁止表达式、未知组件、非法属性和不受支持的颜色值。

### 7.2 依赖范围

插件只处理发布树中 Markdown 实际引用的资源：

- `![[image.png]]`；
- `![alt](relative/image.png)`；
- `[[document.pdf]]`；
- `![[document.pdf#page=3]]`；
- `[说明](relative/document.pdf)`。

其他文件即使位于相同目录也不上传。

### 7.3 图片

- 支持 PNG、JPEG、GIF、BMP、WebP 和 SVG；
- 使用 Obsidian Vault API 解析并读取；
- 上传前检查格式、大小和解析结果；
- 遵守当前接口单图不超过 10 MB 的限制，超限时在预检阶段报错，不在发布中途静默压缩；
- 单轮发布按 SHA-256 去重；
- 发布到引用它的对应页面；
- 资源变化必须触发该页面重新发布。

### 7.4 PDF

- 以 PDF 内容 SHA-256 识别版本；
- 内容未变时复用已导入文件 URL；
- 内容变化时重新导入并更新引用；
- 在智能文档正文中生成链接，不伪造不受支持的附件组件；
- 启用公开只读时，对智能文档与所有已引用 PDF 调用 `manage.set_privilege(policy=2)`；插件永不设置全员可编辑；
- 接口只能确认权限设置请求是否成功。界面把状态显示为“已请求公开”，并提示用户首次发布后用未登录窗口抽查，而不是误报为“已验证公开”；
- PDF 内容变化后产生的新远端文件会替换正文链接；旧版本可能成为孤立文件，记录到诊断列表并由用户手动清理，插件不自动删除。

## 8. 发布预览与执行

### 8.1 只读预览

“预览发布”只进行本地读取和必要的远端查询，不上传图片、不导入 PDF、不编辑页面。

预览按树显示：

- 已绑定且有变化；
- 已绑定且未变化；
- 缺少远端页面；
- 本地链接无法解析；
- 循环或多父引用；
- 远端有人工修改；
- 引用图片和 PDF 的数量；
- 将被忽略的远端页面。

错误必须显示在具体树节点旁，同时在顶部提供可聚焦的错误汇总和恢复操作。

预览分为两档：

- **快速预览**：使用上次缓存的远端树和指纹，只读取本地文件，不消耗新的远端请求；
- **刷新后预览**：显式刷新远端树和页面内容，用于正式发布前的准确预检。

缓存必须显示获取时间。缓存缺失或已过期时，快速预览明确标为“仅本地结果”，不能伪装成完整预检。

### 8.2 发布顺序

1. 重新构建本地页面树；
2. 递归读取远端页面树；
3. 验证所有已选页面的绑定和冲突状态；
4. 计算 Markdown 与资源依赖哈希；
5. 上传变化页面所需图片；
6. 导入或复用 PDF；
7. 完整生成并校验每页 MDX；
8. 按“最深子页面优先、根页面最后”的顺序更新；
9. 请求公开权限并记录结果；
10. 回读页面进行结构检查；
11. 保存状态并显示结果。

先更新子页面、最后更新总览，可以减少用户在发布过程中从入口打开半成品分支的概率。

### 8.3 增量发布

每个页面的发布哈希包含：

- Markdown 正文；
- 被内嵌的 Markdown 内容；
- 引用图片与 PDF 内容；
- 转换器版本；
- 与渲染有关的项目设置。

只有完整哈希未变化且远端未冲突时才能跳过。不能只根据 Markdown 文件修改时间判断。

### 8.4 请求预算、限流与取消

插件以减少腾讯接口调用次数为设计目标：

- 远端页面树按项目缓存，普通浏览和快速预览不自动刷新；
- 正式发布前只刷新已选分支和其必要父节点；
- 同一 Page 的分页结果在一次任务中复用，不重复回读；
- 未变化页面不重复上传资源或写入；
- 读取允许小规模并发，默认并发 2；写入按 Page 串行，同一文档任一时刻只运行一个发布任务；
- 遇到限流或额度不足立即停止新的写入，保留已完成状态和 trace id；
- 用户可以在页面之间取消任务；正在发送的单次请求不能强制回滚，取消后先回读当前 Page 再给出结果。

发布确认页显示预计处理的页面、图片、PDF 数量和预计远端调用范围，但不承诺精确额度消耗，因为服务端计费规则不由插件控制。

## 9. Ctrl+P 命令面板

插件通过 Obsidian `addCommand` 注册命令。用户按 Ctrl+P 打开原生命令面板后可以搜索以下命令：

| 命令名称 | 出现条件 | 行为 |
| --- | --- | --- |
| `腾讯文档发布：发布当前页面` | 当前 Markdown 已绑定到远端 Page | 预检当前页后确认发布 |
| `腾讯文档发布：预览当前页面` | 当前 Markdown 已绑定 | 打开当前页只读预览 |
| `腾讯文档发布：发布当前总览树` | 当前文件属于某个发布项目 | 预检整棵树后确认发布 |
| `腾讯文档发布：预览当前总览树` | 当前文件属于某个发布项目 | 打开树形预览 |
| `腾讯文档发布：创建或管理绑定` | 当前活动文件是 Markdown | 打开绑定界面 |
| `腾讯文档发布：刷新远端页面树` | 当前文件属于某个发布项目 | 重新读取远端 Page 层级并匹配 |
| `腾讯文档发布：打开对应远端文档` | 当前 Markdown 已绑定 | 打开所属智能文档；有可靠直达 URL 时再定位子页面 |
| `腾讯文档发布：打开发布管理` | 始终显示 | 打开项目、状态与最近任务列表 |

实现要求：

- 使用稳定英文 command id，例如 `publish-current-page`，显示名称可以本地化；
- 使用 `checkCallback` 根据活动文件、Token 和绑定状态动态隐藏不可执行命令；
- `checking=true` 时只检查状态，禁止触发网络请求；
- 不默认占用 Ctrl、Alt、Shift 组合键，避免和用户现有快捷键冲突；
- 用户可在 Obsidian“设置 → 快捷键”中自行给常用命令分配快捷键；
- 命令执行后打开与右键操作相同的预览/进度界面，不能维护两套逻辑。

Ctrl+P 是进入 Obsidian 命令面板的方式，不注册成插件自己的快捷键。

腾讯接口目前不返回稳定的子页面直达 URL，因此第一版不得通过猜测 URL 参数跳转子页面。命令名称使用“打开对应远端文档”，打开文档根地址并在提示中显示目标子页面标题。

## 10. 文件树与编辑器右键菜单

Markdown 文件右键：

- `发布当前页面`；
- `预览当前页面`；
- `设为总览并创建发布项目…`；
- `管理页面绑定…`；
- `在腾讯文档中打开`。

文件夹右键：

- `从此文件夹创建发布项目…`；
- `发布此文件夹的总览树`（已有项目时）；
- `预览此文件夹的总览树`；
- `刷新远端页面树`。

编辑器右键只保留最高频的“发布当前页面”和“预览当前页面”，避免菜单过长。

右键、Ctrl+P 和发布管理面板都调用同一组 application service，行为和校验必须一致。

## 11. 绑定与预览界面

### 11.1 首次设置

1. 粘贴 Token；
2. 点击“测试连接”；
3. 选择单页发布或总览发布树；
4. 选择或搜索已有智能文档，或新建一个根智能文档；
5. 递归加载远端页面树；
6. 自动匹配本地与远端树；
7. 用户处理缺失和歧义节点；
8. 运行只读预览；
9. 确认后发布。

Token 输入允许粘贴，不显示完整已保存值，错误紧邻输入框展示。

已有文档优先通过最近列表、精确搜索或已知 file_id 选择。腾讯文档网页 URL 中的标识不保证等于内部 file_id，插件不得仅截取 URL 字符串作为 file_id。新建能力只创建独立智能文档根页面，不承诺创建其子页面。

### 11.2 树映射界面

- 左侧显示本地页面树，右侧显示远端页面树；
- 中间显示“已匹配、待确认、缺失、冲突、未绑定”；
- 支持键盘展开/折叠和选择；
- 路径、标题和错误信息允许换行，不能只靠省略号隐藏关键差异；
- 状态同时使用图标和文字，不只使用颜色；
- 自动匹配后仍允许逐节点重新绑定；
- “保存绑定”不等于“开始发布”，两步分开。

### 11.3 发布进度

进度按页面和阶段显示，例如：

```text
准备 5/5
上传图片 2/3
导入 PDF 1/1
更新页面 3/5：高级功能
验证页面 3/5
```

失败时保留已完成状态、具体阶段和腾讯 trace id，并提供“重试失败页面”“打开远端”“查看本地文件”。

## 12. 设置页

### 12.1 账户

- Token 配置状态；
- 测试连接；
- 替换 Token；
- 清除 Token。

Token 使用 Obsidian `SecretStorage`，不写入 Vault、插件 `data.json` 或日志。

腾讯个人文档服务地址固定为 `https://docs.qq.com/openapi/mcp`，普通设置页不提供任意 Endpoint 输入框，避免 Token 被误发到非腾讯域名。若未来确需支持企业服务或代理，应设计成独立的受信任服务类型，并在发送 Token 前显示目标域名确认，不能复用一个自由文本 URL。

### 12.2 默认发布行为

- 新项目默认是否公开只读；
- 是否将 `![[Markdown]]` 视为子页面；
- 最大递归深度；
- 最大页面数量；
- 冲突时默认停止；
- 页面未变化时跳过。

### 12.3 发布项目

每个项目显示：

- 总览文件；
- 允许跟随的根文件夹；
- 目标智能文档；
- 已绑定页面数和缺失页面数；
- 打开、刷新、管理绑定、移除项目。

“移除项目”只删除本地配置，不删除腾讯文档。

### 12.4 诊断

- 最近发布任务；
- 脱敏日志；
- 复制 trace id；
- 清理本地缓存。

Authorization 请求头和完整 Token 永不进入诊断信息。

## 13. 技术结构

```text
src/
├─ main.ts
├─ types.ts
├─ domain/
│  ├─ hash.ts
│  ├─ local-page-tree.ts
│  ├─ remote-page-tree.ts
│  ├─ page-binding.ts
│  └─ publish-state.ts
├─ obsidian/
│  ├─ link-resolver.ts
│  ├─ vault-reader.ts
│  └─ secret-store.ts
├─ tencent/
│  ├─ errors.ts
│  ├─ mcp-client.ts
│  ├─ mcp-protocol.ts
│  ├─ obsidian-transport.ts
│  ├─ smartcanvas.ts
│  ├─ assets.ts
│  ├─ binary-upload.ts
│  └─ permissions.ts
├─ convert/
│  ├─ markdown-to-mdx.ts
│  └─ remote-mdx-parser.ts
├─ application/
│  ├─ refresh-bindings.ts
│  ├─ preflight.ts
│  ├─ publish-page.ts
│  └─ publish-project.ts
├─ settings/
│  └─ settings-tab.ts
└─ ui/
   ├─ binding-modal.ts
   ├─ overview-picker-modal.ts
   ├─ preview-modal.ts
   ├─ progress-modal.ts
   ├─ project-setup-modal.ts
   └─ project-view.ts
```

测试文件与对应模块放在同一目录，以 `*.test.ts` 命名。仓库根目录保留 Obsidian 必需清单、样式、构建配置、版本脚本和 GitHub Release 工作流；生成的 `main.js` 不提交到 Git，而是作为 Release 资产发布。

技术要求：

- TypeScript + Obsidian 官方 API；
- `requestUrl` 访问腾讯 MCP，支持 JSON 与 SSE 响应；
- `Vault.cachedRead/readBinary` 读取文件；
- `MetadataCache` 解析链接和源位置；
- Web Crypto 计算 SHA-256；
- 不读取 mcporter 配置，不要求安装 skill 或额外命令行工具；
- 不使用 Node `fs/path/crypto`，为移动端兼容保留条件；
- MCP 客户端固定走 HTTPS，完成 `initialize` 与 `notifications/initialized`，保存 `mcp-session-id`，并兼容 JSON 与 `text/event-stream` 响应；
- 日志只记录工具名、阶段、耗时、脱敏错误和 trace id，不记录请求头、Token、上传 URL、正文或资源 base64；
- 使用 `SecretStorage` 要求最低 Obsidian 版本为 1.11.4，`manifest.json` 与 `versions.json` 必须一致；
- `obsidian` 类型依赖固定为已经验证的 1.13.1，TypeScript 开启完整 `strict` 检查；
- `manifest.json` 保持 `isDesktopOnly: true`，真实移动端验收完成前不承诺移动端支持；
- GitHub Release 标签、名称、`package.json` 与发布资产中的 `manifest.json` 版本必须一致；
- 页面解析必须保留 Page、readonly 和 Unsupported 块；
- 所有命令、右键和面板共用同一发布服务。

## 14. 第一版范围

必须完成：

- Token 配置与连接测试；
- 选择已有或新建根智能文档；
- 单个 Markdown ↔ 根页面/已有子页面；
- 总览 Markdown 递归构建本地页面树；
- 递归读取腾讯已有子页面树；
- 树形自动匹配、手动绑定和刷新；
- 图片和 PDF 引用；
- 真正只读的发布预览；
- 只更新发生变化的页面；
- 更新父页面时保留所有已有子页面；
- 冲突提示、任务内恢复快照和发布后验证；
- Ctrl+P 命令、文件树右键、编辑器右键和发布管理；
- 可选请求智能文档与 PDF 全员可读，并明确展示“已请求/待人工抽查”。

暂不实现：

- 自动创建、移动、重命名或删除腾讯子页面；
- 双向同步；
- 自动发布或 Git 钩子；
- 除图片和 PDF 外的附件；
- Word 模式；
- 精确还原子页面卡片在父 Markdown 中的行内位置。

## 15. 验收标准

1. 用户只安装插件并填写 Token 即可使用。
2. 一篇总览 Markdown 能按链接出现顺序生成稳定的本地页面树。
3. 两层及以上的本地链接能够匹配两层及以上的已有腾讯子页面。
4. 更新任意父页面不会删除、移动或破坏其子页面。
5. 当前活动 Markdown 可以通过 Ctrl+P 发布、预览、绑定和打开远端页面。
6. 预览操作不产生任何远端写入。
7. 超过 20 个 Block 的页面可以通过分页完整读取、比较和验证，不发生静默截断。
8. 只处理页面树中的 Markdown 及其实际引用的图片和 PDF。
9. 图片或 PDF 改变时能触发正确页面更新。
10. 缺失页面、循环、歧义映射和远端内容指纹冲突会在写入前明确显示。
11. 发布中断或响应不明确时先回读判定，不盲目重复写入。
12. 公开模式下，智能文档和引用 PDF 均成功请求全员可读，并提示首次人工抽查。
13. Token 不出现在 Vault、Git、控制台或诊断日志中，也不会发送到用户可编辑的任意 Endpoint。
14. 替换成另一个账号的 Token 后，旧项目在重新验证前不能写入。
15. 插件所有用户可见文字、命令和示例保持通用。

## 16. 实施顺序

以下顺序已经用于首版实现；完成标记指代码与自动化测试完成，不替代真实服务验收：

1. [x] 整理插件骨架和清单版本，建立 SecretStorage、数据迁移、设置页和稳定 command id。
2. [x] 实现直连腾讯 HTTPS MCP 的客户端、脱敏错误模型和只读连接测试。
3. [x] 实现指定 Page 的完整分页读取、远端 MDX 安全解析与内容指纹。
4. [x] 实现本地链接树与远端 Page 树递归解析。
5. [x] 实现树形绑定界面、远端缓存和“刷新远端页面树”。
6. [x] 实现单页快速预览、刷新后预检与发布，并保证保留子 Page 块。
7. [x] 加入非事务失败恢复、冲突确认和发布后分页回读验证。
8. [x] 实现整棵树增量发布、图片、PDF、请求预算和公开权限请求。
9. [x] 接入 Ctrl+P、文件树右键、编辑器右键和发布管理面板。
10. [ ] 在真实 Obsidian Vault 和腾讯测试文档中完成至少两层子页面、超过 20 个 Block、远端人工修改和中途失败场景验收；对应模拟端到端测试已完成。

## 17. 仓库整理与发布结构

开始网络和发布逻辑前完成的整理如下：

- [x] `manifest.json` 改为单向发布描述，并与 `versions.json` 统一最低 Obsidian 版本 1.11.4；
- [x] 移除自定义 MCP Endpoint、默认空间、自动保存同步和本机 `localhost` 默认值；
- [x] 网络请求固定到腾讯 HTTPS MCP 地址；
- [x] `README.md` 重写为 Obsidian 插件说明，并统一使用“发布”术语；
- [x] 删除 `legacy-sync/` 和旧同步器缓存，不再发布或参与构建；
- [x] 固定 `obsidian` 类型依赖到 1.13.1，并启用完整 TypeScript `strict` 检查；
- [x] 增加 ESLint、EditorConfig、版本同步脚本、许可证和变更日志；
- [x] 配置公开 GitHub 仓库 `totok22/tencent-docs-publisher`；
- [x] 增加 BRAT 兼容的 GitHub Release 工作流，发布 `main.js`、`manifest.json` 和 `styles.css`；
- [x] 创建并发布首个 `0.1.0` 预发布版，三个 BRAT 安装资产已通过下载、摘要和语法验证；
- [ ] 在真实 Obsidian Vault 中通过 BRAT 完成安装、启用和基本操作验证。

## 18. 测试策略

当前自动化测试为 7 个测试文件、46 个测试用例，覆盖：

- 本地 wikilink、Markdown link、嵌入、循环、多父引用、同名文件和跨范围链接的树解析；
- 远端 MDX 的直接/嵌套 Page 提取、分页拼接、readonly/Unsupported 保留和内容指纹稳定性；
- Markdown 表格、公式、callout、图片、PDF 与不支持语法的转换结果；
- JSON/SSE 两种 MCP 响应、会话 ID、超时、限流、额度不足、响应丢失和重复请求保护；
- 数据版本迁移、Token 更换账号、缓存过期和项目移除不影响远端内容。

模拟端到端测试已经覆盖两层页面树、超过 20 个 Block、额外未绑定远端页面和发布写入。真实端到端验收必须使用专门的通用测试文档，不使用真实项目资料，并至少包含：

1. 根页面、两层子页面和同名不同父级页面；
2. 单页超过 20 个 Block，以验证分页；
3. 重复图片、变更后的 PDF、超过 10 MB 的图片；
4. 远端人工修改、页面移动、页面缺失和额外未绑定页面；
5. 写入中途失败、取消、发布后回读不一致；
6. 全员可读请求成功后，使用未登录浏览器人工抽查文档和 PDF。

如果插件保持 `isDesktopOnly: false`，发布前还必须在至少一个移动端 Vault 验证 Token 存储、图片读取、PDF 上传和任务取消；否则应将清单明确改为仅桌面端，不能保留未经测试的移动端承诺。
